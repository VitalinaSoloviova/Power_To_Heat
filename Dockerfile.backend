# Backend - Express + TypeScript
FROM node:20-alpine

WORKDIR /app

# Copy package files
COPY package*.json ./
RUN npm ci --frozen-lockfile

# Copy backend code
COPY database ./database

# Build if needed (or run with tsx)
RUN npm install -g tsx

EXPOSE 3001

CMD ["tsx", "database/api/rest-server.ts"]